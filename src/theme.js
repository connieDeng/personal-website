const white = '#f5f5f5'
const black = '#242426'

export const LightTheme = {
    pageBackground: white,
    titleColor: black,
    divColor: black,
};
  
export const DarkTheme = {
    pageBackground: black,
    titleColor: white,
    divColor: white,
}

export const themes = {
    light: LightTheme,
    dark: DarkTheme,
}
